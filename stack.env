# Tido Environment Configuration for Portainer/Unraid
# This file defines environment variables for stack deployment

# Node environment (production recommended)
NODE_ENV=production

# Port the application runs on (default: 3000)
PORT=3000

# User/Group IDs for file permissions (Unraid defaults)
# PUID=99 and PGID=100 are standard for Unraid
# For other systems, use your user's UID/GID (run 'id' command to find yours)
PUID=1000
PGID=1000

# Database path (optional - defaults to /app/data/todos.db)
# Only change if you need a custom location
# DB_PATH=/app/data/todos.db
