# Tido Environment Configuration for Portainer/Unraid
# This file defines environment variables for stack deployment

# Node environment (production recommended)
NODE_ENV=production

# Port the application runs on (default: 3000)
PORT=3000

# Cookie security settings
# IMPORTANT: Set to 'false' if you're accessing via HTTP (not HTTPS)
# If you're behind a reverse proxy with HTTPS, keep this commented out (defaults to true)
COOKIE_SECURE=false

# User/Group IDs for file permissions
# IMPORTANT: These control who owns the database and upload files
#
# For Unraid: Use PUID=99 and PGID=100
# For other systems: Use your user's UID/GID (run 'id' command to find yours)
# For Docker Desktop: Use PUID=1000 and PGID=1000
PUID=1000
PGID=1000

# Database path (optional - defaults to /app/data/todos.db)
# Only change if you need a custom location
# DB_PATH=/app/data/todos.db

# DEBUGGING:
# If container is unhealthy or won't start:
# 1. Check logs: docker logs tido-app
# 2. View startup messages: docker logs tido-app | grep "Starting Tido"
# 3. Verify volume mount: docker exec tido-app ls -la /app/data
# 4. Check permissions: docker exec tido-app ls -la /app/data
